# Temporary HTTP-only site for obtaining the first certificate. No RAG proxy.
server {
    listen 80;
    listen [::]:80;
    server_name rag.maiaplatform.org;
    location /.well-known/acme-challenge/ { root /var/www/html; }
    location / { return 404; }
}
