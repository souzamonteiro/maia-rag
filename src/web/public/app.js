async function refresh(){const s=await fetch('/api/stats').then(r=>r.json());document.querySelector('#stats').innerHTML=Object.entries(s).map(([k,v])=>`<div class="card"><span>${k}</span><b>${v}</b></div>`).join('');const docs=await fetch('/api/documents').then(r=>r.json());document.querySelector('#docs').innerHTML=docs.map(d=>`<tr><td>${d.filename}</td><td>${d.status}</td><td>${d.chunk_count}</td><td>${d.aiMetadata?.documentType||''}<br><small>${(d.aiMetadata?.domains||[]).map(x=>x.name).join(', ')}</small></td></tr>`).join('')}
async function upload(file){const fd=new FormData();fd.append('file',file,file.name);document.querySelector('#result').textContent='Processing...';const r=await fetch('/api/documents',{method:'POST',body:fd});document.querySelector('#result').textContent=JSON.stringify(await r.json(),null,2);refresh()}
const drop=document.querySelector('#drop');drop.ondragover=e=>{e.preventDefault();drop.classList.add('drag')};drop.ondragleave=()=>drop.classList.remove('drag');drop.ondrop=e=>{e.preventDefault();drop.classList.remove('drag');[...e.dataTransfer.files].forEach(upload)};document.querySelector('#file').onchange=e=>[...e.target.files].forEach(upload);refresh();
